service muffyn-sh /vendor/bin/init.muffyn.sh
    class late_start
    user root
    group root system radio
    oneshot

on property:sys.boot_completed=1
    # Restrict permissions to socket file
    # to hide Magisk & co.
    chmod 440 /proc/net/unix
